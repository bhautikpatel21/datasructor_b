#include <stdio.h>

int a[5]={2,56,31,47,9},i,mid;

int binary() 
{
    printf ("|| : This is a array : || \n      ");

    for (i=0; i<=4; i++)
    {
        printf ("%d ",a[i]);
    }
    
    for (i=0; i<=4; i++)
    {
    mid=(0+4)/2;
    }
    if (a[mid]==31)
    {
        printf ("\n|| : That is a fair elemnt : || ");
    }
    else
    {
        printf ("|| NOt show ||");
    }

}
int main ()
{
    binary();
}